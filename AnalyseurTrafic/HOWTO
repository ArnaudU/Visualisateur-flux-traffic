Java 10 ou plus necessaire pour executer ce programme

Ce programme neccessite un /bin dans le repertoire AnalyseurTrafic assurer vous de l'avoir creer dans le repertoire AnalyseurTrafic avant en faisant:
    $~mkdir bin
Pour executer ce programme ecrire sur un terminal l'une des commandes suivantes(a vous de choisir) dans le repertoire AnalyseurTrafic:
    $~chmod +x analyseur.sh
    $~./analyseur.sh
Ou bien:
    $~make analyseur
Si la commande make ne fonctionne pas faite dans le repertoire AnalyseurTrafic:
    $~javac -sourcepath src -d bin ./src/protocole/*.java ./src/traitement/*.java ./src/viewer/*.java ./src/main/*.java
	$~java -cp bin main.Main
Lorsque la fenetre est apparu vous pourrez choisir 2 possibilite:
    -le path du .txt à inserer dans le champs texte et puis appuyer sur le bouton decode 0
    -appuyer sur le bouton path qui affichera une nouvelle fenetre ou vous pourrez choisir le fichier et affichera une erreur 
     si le fichier est errone/tronque ou sinon affichera la table sur l'application

Ensuite il y a le bouton save qui permet de sauvegarder les trames sous format txt
Et le bouton "save details" qui permet de sauvegarder les trames detailles(comme sur wireshark) sous format txt

Concernant le filtre, on peut l'utiliser en indicant dans le champs a gauche du bouton filtre ceux que l'on veut filtre,on peux filtrer seulement:
    -le protocole 
    -l'addresse ip source 
    - on peut combiner en faisant un "&&" ou "||" qui permet respectivement de faire un "et logique" et un "ou logique"
Voici un exemple:
    On veut filtrer une ip 155.155.125.4 et un protocole http, on fait:
        -"155.155.125.4 && http"